# Middleware Folder

Contains custom Express middleware modules:
- auth.js: Authenticates requests using Firebase Admin SDK to decode JWTs.
- errorHandler.js: Custom app-wide fallback error middleware.
- validation.js: Request body/param validators.
