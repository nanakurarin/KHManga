# Controllers Folder

Contains controller modules to parse requests and construct JSON responses:
- authController.js: Manages user synchronization on signup/login.
- bookmarkController.js: Controls list/add/delete actions on user bookmark records.
- historyController.js: Updates and retrieves chapters user has read.
