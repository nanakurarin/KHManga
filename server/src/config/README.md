# Config Folder

Contains initialization code for libraries and external clients:
- db.js: Configures the PrismaClient instance and exports it.
- firebase-config.js: Initializes Firebase Admin SDK using application credentials.
