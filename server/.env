# Server Environment Configuration
PORT=5000
NODE_ENV=development

# MySQL Database URL for Prisma (replace with actual db user, password, host, port, and db name)
DATABASE_URL="mysql://root:password@localhost:3306/khmanga_db"

# Firebase Admin SDK Configuration (JSON string or path to config file)
FIREBASE_SERVICE_ACCOUNT_JSON=""
